version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - upload_files:/tmp/file_upload
    environment:
      - UPLOAD_PATH=/tmp/file_upload
    restart: unless-stopped

volumes:
  upload_files:
    driver: local